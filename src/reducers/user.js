export const initialState = {
        name: "Maks",
        surname: "Usenko",
        following: 3,
        followers: 45,
        balance: 2455
};

		
export function userReducer(state = initialState) {
	return state
}
